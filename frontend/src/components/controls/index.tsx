import * as React from "react";
import { Box, Stack } from "@chakra-ui/core";

export const Controls: React.FC = () => {
  return (
    <Box>
      <Stack>
        <Box height={6} bg="gray.300" />
      </Stack>
    </Box>
  );
};
